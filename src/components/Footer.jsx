import Navbar from './Navbar';

const Footer = () => {
    return (
        <footer>
            <Navbar />
            <small>@2024 La Cara de los Últimos</small>
        </footer>
    );
};

export default Footer;
